server:
  port: 8080

spring:
  application:
    name: pisces
  datasource:
    url: jdbc:h2:file:./data/pisces;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration
